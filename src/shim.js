// Re-exports auto-injected by esbuild --inject wherever the bundle
// references `Buffer` or `process` as bare globals.
export { Buffer } from 'buffer';
export { default as process } from 'process/browser';
